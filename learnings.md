- [2026-09-05] -> Tailwind v4: una regla de elemento (h1,h2,h3) escrita FUERA de @layer en globals.css gana a las utilidades font-_/color-_ de @layer utilities · Por qué importa: la utilidad SÍ se genera en el CSS compilado pero nunca se aplica, y el síntoma (la clase existe, el elemento no cambia) parece un bug de compilación cuando es cascada · Principio: en Tailwind v4 todo estilo base propio va envuelto en @layer base; el CSS sin capa siempre vence al CSS con capa.
- [2026-09-05] -> GSAP .from() escribe el estado inicial (opacity:0) en el atributo style del elemento; si el ticker no arranca —pestaña en segundo plano con rAF suspendido, fallo de carga de la librería— la pantalla queda permanentemente en blanco · Por qué importa: se diagnostica como bug de layout o de datos y no lo es; además es un fallo real en móviles lentos, no solo en pruebas · Principio: para entradas simples (fade/rise de 3-5 elementos) usar @keyframes CSS con animation-fill-mode:both, que el navegador garantiza que terminan; reservar GSAP para coreografías que CSS no puede expresar.
- [2026-09-05] -> Tailwind v4: una regla de elemento (h1,h2,h3) escrita FUERA de @layer en globals.css gana a las utilidades font-_/color-_ de @layer utilities · Por qué importa: la utilidad SÍ se genera en el CSS compilado pero nunca se aplica, y el síntoma (la clase existe, el elemento no cambia) parece un bug de compilación cuando es cascada · Principio: en Tailwind v4 todo estilo base propio va envuelto en @layer base; el CSS sin capa siempre vence al CSS con capa.
- [2026-09-05] -> GSAP .from() escribe el estado inicial (opacity:0) en el atributo style del elemento; si el ticker no arranca —pestaña en segundo plano con rAF suspendido, fallo de carga de la librería— la pantalla queda permanentemente en blanco · Por qué importa: se diagnostica como bug de layout o de datos y no lo es; además es un fallo real en móviles lentos, no solo en pruebas · Principio: para entradas simples (fade/rise de 3-5 elementos) usar @keyframes CSS con animation-fill-mode:both, que el navegador garantiza que terminan; reservar GSAP para coreografías que CSS no puede expresar.
- [2026-09-05] -> Tailwind v4: una regla de elemento (h1,h2,h3) escrita FUERA de @layer en globals.css gana a las utilidades font-_/color-_ de @layer utilities · Por qué importa: la utilidad SÍ se genera en el CSS compilado pero nunca se aplica, y el síntoma (la clase existe, el elemento no cambia) parece un bug de compilación cuando es cascada · Principio: en Tailwind v4 todo estilo base propio va envuelto en @layer base; el CSS sin capa siempre vence al CSS con capa.
- [2026-09-05] -> GSAP .from() escribe el estado inicial (opacity:0) en el atributo style del elemento; si el ticker no arranca —pestaña en segundo plano con rAF suspendido, fallo de carga de la librería— la pantalla queda permanentemente en blanco · Por qué importa: se diagnostica como bug de layout o de datos y no lo es; además es un fallo real en móviles lentos, no solo en pruebas · Principio: para entradas simples (fade/rise de 3-5 elementos) usar @keyframes CSS con animation-fill-mode:both, que el navegador garantiza que terminan; reservar GSAP para coreografías que CSS no puede expresar.
- [2026-09-05] -> Tailwind v4: una regla de elemento (h1,h2,h3) escrita FUERA de @layer en globals.css gana a las utilidades font-_/color-_ de @layer utilities · Por qué importa: la utilidad SÍ se genera en el CSS compilado pero nunca se aplica, y el síntoma (la clase existe, el elemento no cambia) parece un bug de compilación cuando es cascada · Principio: en Tailwind v4 todo estilo base propio va envuelto en @layer base; el CSS sin capa siempre vence al CSS con capa.
- [2026-09-05] -> GSAP .from() escribe el estado inicial (opacity:0) en el atributo style del elemento; si el ticker no arranca —pestaña en segundo plano con rAF suspendido, fallo de carga de la librería— la pantalla queda permanentemente en blanco · Por qué importa: se diagnostica como bug de layout o de datos y no lo es; además es un fallo real en móviles lentos, no solo en pruebas · Principio: para entradas simples (fade/rise de 3-5 elementos) usar @keyframes CSS con animation-fill-mode:both, que el navegador garantiza que terminan; reservar GSAP para coreografías que CSS no puede expresar.
- [2026-09-05] -> Tailwind v4: una regla de elemento (h1,h2,h3) escrita FUERA de @layer en globals.css gana a las utilidades font-_/color-_ de @layer utilities · Por qué importa: la utilidad SÍ se genera en el CSS compilado pero nunca se aplica, y el síntoma (la clase existe, el elemento no cambia) parece un bug de compilación cuando es cascada · Principio: en Tailwind v4 todo estilo base propio va envuelto en @layer base; el CSS sin capa siempre vence al CSS con capa.
- [2026-09-05] -> GSAP .from() escribe el estado inicial (opacity:0) en el atributo style del elemento; si el ticker no arranca —pestaña en segundo plano con rAF suspendido, fallo de carga de la librería— la pantalla queda permanentemente en blanco · Por qué importa: se diagnostica como bug de layout o de datos y no lo es; además es un fallo real en móviles lentos, no solo en pruebas · Principio: para entradas simples (fade/rise de 3-5 elementos) usar @keyframes CSS con animation-fill-mode:both, que el navegador garantiza que terminan; reservar GSAP para coreografías que CSS no puede expresar.
- [2026-09-05] -> Tailwind v4: una regla de elemento (h1,h2,h3) escrita FUERA de @layer en globals.css gana a las utilidades font-_/color-_ de @layer utilities · Por qué importa: la utilidad SÍ se genera en el CSS compilado pero nunca se aplica, y el síntoma (la clase existe, el elemento no cambia) parece un bug de compilación cuando es cascada · Principio: en Tailwind v4 todo estilo base propio va envuelto en @layer base; el CSS sin capa siempre vence al CSS con capa.
- [2026-09-05] -> GSAP .from() escribe el estado inicial (opacity:0) en el atributo style del elemento; si el ticker no arranca —pestaña en segundo plano con rAF suspendido, fallo de carga de la librería— la pantalla queda permanentemente en blanco · Por qué importa: se diagnostica como bug de layout o de datos y no lo es; además es un fallo real en móviles lentos, no solo en pruebas · Principio: para entradas simples (fade/rise de 3-5 elementos) usar @keyframes CSS con animation-fill-mode:both, que el navegador garantiza que terminan; reservar GSAP para coreografías que CSS no puede expresar.
- [2026-09-05] -> Tailwind v4: una regla de elemento (h1,h2,h3) escrita FUERA de @layer en globals.css gana a las utilidades font-_/color-_ de @layer utilities · Por qué importa: la utilidad SÍ se genera en el CSS compilado pero nunca se aplica, y el síntoma (la clase existe, el elemento no cambia) parece un bug de compilación cuando es cascada · Principio: en Tailwind v4 todo estilo base propio va envuelto en @layer base; el CSS sin capa siempre vence al CSS con capa.
- [2026-09-05] -> GSAP .from() escribe el estado inicial (opacity:0) en el atributo style del elemento; si el ticker no arranca —pestaña en segundo plano con rAF suspendido, fallo de carga de la librería— la pantalla queda permanentemente en blanco · Por qué importa: se diagnostica como bug de layout o de datos y no lo es; además es un fallo real en móviles lentos, no solo en pruebas · Principio: para entradas simples (fade/rise de 3-5 elementos) usar @keyframes CSS con animation-fill-mode:both, que el navegador garantiza que terminan; reservar GSAP para coreografías que CSS no puede expresar.
- [2026-09-05] -> Tailwind v4: una regla de elemento (h1,h2,h3) escrita FUERA de @layer en globals.css gana a las utilidades font-_/color-_ de @layer utilities · Por qué importa: la utilidad SÍ se genera en el CSS compilado pero nunca se aplica, y el síntoma (la clase existe, el elemento no cambia) parece un bug de compilación cuando es cascada · Principio: en Tailwind v4 todo estilo base propio va envuelto en @layer base; el CSS sin capa siempre vence al CSS con capa.
- [2026-09-05] -> GSAP .from() escribe el estado inicial (opacity:0) en el atributo style del elemento; si el ticker no arranca —pestaña en segundo plano con rAF suspendido, fallo de carga de la librería— la pantalla queda permanentemente en blanco · Por qué importa: se diagnostica como bug de layout o de datos y no lo es; además es un fallo real en móviles lentos, no solo en pruebas · Principio: para entradas simples (fade/rise de 3-5 elementos) usar @keyframes CSS con animation-fill-mode:both, que el navegador garantiza que terminan; reservar GSAP para coreografías que CSS no puede expresar.
- [2026-09-05]  -> Tailwind v4: una regla de elemento (h1,h2,h3) escrita FUERA de @layer en globals.css gana a las utilidades font-*/color-* de @layer utilities · Por qué importa: la utilidad SÍ se genera en el CSS compilado pero nunca se aplica, y el síntoma (la clase existe, el elemento no cambia) parece un bug de compilación cuando es cascada · Principio: en Tailwind v4 todo estilo base propio va envuelto en @layer base; el CSS sin capa siempre vence al CSS con capa.
- [2026-09-05]  -> GSAP .from() escribe el estado inicial (opacity:0) en el atributo style del elemento; si el ticker no arranca —pestaña en segundo plano con rAF suspendido, fallo de carga de la librería— la pantalla queda permanentemente en blanco · Por qué importa: se diagnostica como bug de layout o de datos y no lo es; además es un fallo real en móviles lentos, no solo en pruebas · Principio: para entradas simples (fade/rise de 3-5 elementos) usar @keyframes CSS con animation-fill-mode:both, que el navegador garantiza que terminan; reservar GSAP para coreografías que CSS no puede expresar.
- [2026-09-05]  -> Tailwind v4: una regla de elemento (h1,h2,h3) escrita FUERA de @layer en globals.css gana a las utilidades font-*/color-* de @layer utilities · Por qué importa: la utilidad SÍ se genera en el CSS compilado pero nunca se aplica, y el síntoma (la clase existe, el elemento no cambia) parece un bug de compilación cuando es cascada · Principio: en Tailwind v4 todo estilo base propio va envuelto en @layer base; el CSS sin capa siempre vence al CSS con capa.
- [2026-09-05]  -> GSAP .from() escribe el estado inicial (opacity:0) en el atributo style del elemento; si el ticker no arranca —pestaña en segundo plano con rAF suspendido, fallo de carga de la librería— la pantalla queda permanentemente en blanco · Por qué importa: se diagnostica como bug de layout o de datos y no lo es; además es un fallo real en móviles lentos, no solo en pruebas · Principio: para entradas simples (fade/rise de 3-5 elementos) usar @keyframes CSS con animation-fill-mode:both, que el navegador garantiza que terminan; reservar GSAP para coreografías que CSS no puede expresar.
